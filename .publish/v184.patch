diff --git a/.gitignore b/.gitignore
new file mode 100644
index 0000000..b0cbedc
--- /dev/null
+++ b/.gitignore
@@ -0,0 +1,4 @@
+__pycache__/
+*.py[cod]
+*.bak
+recon_*/
diff --git a/README.md b/README.md
index 1256840..365b468 100644
--- a/README.md
+++ b/README.md
@@ -25,13 +25,15 @@ l1ght_recon -t 192.168.92.206 --vhost-domain alvo.local
 l1ght_recon --check-update
 ```
 
-`--log` grava um log detalhado para auditoria, incluindo comandos, stdout/stderr, códigos de retorno, duração das etapas e exceções. Cookies e segredos conhecidos são redigidos no log.
+`--log` grava um log detalhado para auditoria, incluindo comandos, stdout/stderr, códigos de retorno, duração das etapas e exceções. Cookies e segredos conhecidos são redigidos no log. Sem indicar um nome, o arquivo fica em `recon_<host>_<data_hora>/debug.log`, e o caminho completo também é exibido na seção **ARTEFATOS** ao final.
 
 O relatório final é gerado em **HTML**. Ele contém detalhes adicionais que não são exibidos no terminal para manter a execução mais limpa e fluida.
 
 ## Atualização
 
-A ferramenta verifica silenciosamente se há uma versão estável mais nova. Se estiver atualizada ou não houver conectividade, não imprime mensagem. Quando existe atualização, valida SHA-256 e sintaxe antes de substituir o script.
+A ferramenta verifica silenciosamente se há uma versão estável mais nova **antes de processar os argumentos da execução normal**. Isso permite atualizar primeiro e só depois interpretar parâmetros adicionados por versões novas. Se estiver atualizada ou não houver conectividade, não imprime mensagem. Quando existe atualização, valida SHA-256 e sintaxe antes de substituir o script.
+
+`--check-update` agora também instala imediatamente a versão mais nova quando ela existe. `--update` é mantido como comando equivalente por compatibilidade. O updater não deixa arquivos `.bak` persistentes; backups legados criados por versões anteriores são removidos automaticamente.
 
 ```bash
 l1ght_recon --check-update
diff --git a/l1ght_recon.py b/l1ght_recon.py
index 5a2580f..68faecd 100644
--- a/l1ght_recon.py
+++ b/l1ght_recon.py
@@ -65,7 +65,7 @@ urllib3.disable_warnings(urllib3.exceptions.InsecureRequestWarning)
 # Scanning & Enumeration
 # ============================================================
 
-VERSION = "1.8.3"
+VERSION = "1.8.4"
 AUTHOR = "Rafael Ademilton"
 HANDLE = "l1ghtr3v3n"
 
@@ -329,17 +329,9 @@ def _install_update(manifest):
         manifest["sha256"],
     )
 
-    backup_path = script_path.with_name(
-        f"{script_path.name}.v{VERSION}.bak"
-    )
-
-    try:
-        shutil.copy2(script_path, backup_path)
-    except Exception as exc:
-        raise RuntimeError(
-            f"não foi possível criar o backup em {backup_path}: {exc}"
-        ) from exc
-
+    # A atualização já foi validada (HTTPS, SHA-256, VERSION e sintaxe)
+    # antes deste ponto. Como os.replace() é atômico no mesmo filesystem,
+    # não mantemos .bak persistente no diretório do usuário.
     temp_path = None
 
     try:
@@ -366,7 +358,34 @@ def _install_update(manifest):
             except Exception:
                 pass
 
-    return script_path, backup_path
+    return script_path
+
+
+def cleanup_legacy_update_backups():
+    """Remove apenas backups legados criados pelo updater do L1ght Recon."""
+    script_path = _running_script_path()
+    patterns = (
+        f"{script_path.name}.v*.bak",
+        f"{script_path.name}.bak",
+    )
+    removed = []
+
+    for pattern in patterns:
+        for candidate in script_path.parent.glob(pattern):
+            try:
+                if candidate.is_file() or candidate.is_symlink():
+                    candidate.unlink()
+                    removed.append(candidate)
+            except Exception as exc:
+                _debug_log("UPDATE_CLEANUP", f"falha removendo {candidate}: {exc}")
+
+    if removed:
+        _debug_log(
+            "UPDATE_CLEANUP",
+            "backups legados removidos: " + ", ".join(str(x) for x in removed),
+        )
+
+    return removed
 
 
 def _restart_after_update(script_path):
@@ -396,7 +415,8 @@ def handle_update_check(
       - silencioso quando não há internet/manifesto;
       - só informa algo quando existe atualização real.
 
-    Modo verbose é usado por --check-update e --update.
+    Modo verbose é usado por --check-update e --update. Ambos instalam
+    uma versão mais nova quando ela estiver disponível.
     """
     if os.environ.pop(UPDATE_SKIP_ONCE_ENV, None):
         return "skipped"
@@ -442,7 +462,7 @@ def handle_update_check(
         return "available"
 
     try:
-        script_path, backup_path = _install_update(manifest)
+        script_path = _install_update(manifest)
     except Exception as exc:
         print(
             f"{YELLOW}[!]{RESET} Não foi possível instalar a atualização; "
@@ -455,9 +475,6 @@ def handle_update_check(
         f"v{VERSION} -> v{remote_version}"
     )
 
-    if verbose:
-        print(f"{DIM}    Backup: {backup_path}{RESET}")
-
     if restart:
         print(
             f"{CYAN}[*]{RESET} Reiniciando o L1ght Recon com a nova versão..."
@@ -584,6 +601,38 @@ def wait_future_with_progress(future, label, interval=15):
             info(f"{label} continua em execução em segundo plano...")
 
 
+def preparse_auto_update():
+    """
+    Faz a checagem automática antes do argparse.
+
+    Isso permite que uma versão antiga se atualize antes de rejeitar um
+    parâmetro introduzido por uma versão nova (por exemplo, --log).
+    A execução é silenciosa quando não há atualização ou não há Internet.
+    """
+    raw_args = set(sys.argv[1:])
+
+    if not raw_args:
+        return "skipped"
+
+    skip_flags = {
+        "-h",
+        "--help",
+        "--flow",
+        "--no-update",
+        "--check-update",
+        "--update",
+    }
+
+    if raw_args.intersection(skip_flags):
+        return "skipped"
+
+    return handle_update_check(
+        install=True,
+        restart=True,
+        verbose=False,
+    )
+
+
 # ============================================================
 # Visual
 # ============================================================
@@ -5517,6 +5566,8 @@ def print_final_compilation(
     print(f"{BOLD}ARTEFATOS{RESET}")
     print(f"  Diretório...............: {output_dir}")
     print(f"  Relatório HTML..........: {html_report}")
+    if DEBUG_LOG_PATH is not None:
+        print(f"  Log detalhado...........: {DEBUG_LOG_PATH}")
     print(f"  {DIM}O HTML contém detalhes adicionais que não são exibidos no terminal.{RESET}")
 
     print()
@@ -5534,6 +5585,10 @@ def print_final_compilation(
 # ============================================================
 
 def main():
+    # Antes do argparse: uma versão antiga pode se atualizar e reiniciar
+    # antes de rejeitar flags novas ainda desconhecidas localmente.
+    preparse_auto_update()
+
     examples = """
 Exemplos:
   python3 l1ght_recon.py -t 192.168.92.206
@@ -5742,8 +5797,8 @@ Fluxo:
         "--check-update",
         action="store_true",
         help=(
-            "Verifica se existe uma versão mais nova e encerra. "
-            "Não instala a atualização."
+            "Verifica e instala automaticamente uma versão mais nova, "
+            "se existir, e encerra."
         ),
     )
     update_group.add_argument(
@@ -5770,14 +5825,14 @@ Fluxo:
 
     if args.check_update:
         handle_update_check(
-            install=False,
+            install=True,
             restart=False,
             verbose=True,
         )
         return
 
     if args.update:
-        update_status = handle_update_check(
+        handle_update_check(
             install=True,
             restart=bool(args.target),
             verbose=True,
@@ -5786,15 +5841,7 @@ Fluxo:
         if not args.target:
             return
 
-    elif not args.no_update and not args.flow:
-        # Checagem automática deliberadamente silenciosa:
-        # sem internet, manifesto ausente ou versão atual não poluem a saída.
-        handle_update_check(
-            install=True,
-            restart=True,
-            verbose=False,
-        )
-
+    # A checagem automática das execuções normais já ocorreu antes do argparse.
     banner()
 
     if args.flow:
@@ -5846,6 +5893,8 @@ Fluxo:
         init_debug_log(log_path, cookie=cookie)
         info(f"Log detalhado habilitado: {DEBUG_LOG_PATH}")
 
+    # Remove .bak deixados por versões anteriores do updater.
+    cleanup_legacy_update_backups()
     ensure_path_command()
     tools = check_external_tools(auto_setup=True)
 
diff --git a/requirements.txt b/requirements.txt
index 16c8c1e..cabe0ea 100644
--- a/requirements.txt
+++ b/requirements.txt
@@ -1,3 +1,3 @@
-# L1ght Recon v1.8.3
+# L1ght Recon v1.8.4
 requests>=2.32.0
 beautifulsoup4>=4.12.0
